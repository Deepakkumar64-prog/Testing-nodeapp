{
  "name": "my-node-app",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build process required'",
    "test": "echo 'No tests specified'"  // Yahan yeh script add karein
  },
  "dependencies": {
    // Aapke dependencies yahan
  }
}
